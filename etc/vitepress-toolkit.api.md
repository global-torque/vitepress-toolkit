## API Report File for "@global-torque/vitepress-toolkit"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { FrontmatterInput } from '@global-torque/content-toolkit/pageData';
import { NormalizedFrontmatter } from '@global-torque/content-toolkit/pageData';
import { NormalizeFrontmatterOptions } from '@global-torque/content-toolkit/pageData';
import { PageDataLike } from '@global-torque/content-toolkit/pageData';

// @public
export function createFontPreloadTransform(input: CreateFontPreloadTransformOptions): (input: TransformHeadContext) => FontPreloadHeadEntry[] | undefined;

// @public
export interface CreateFontPreloadTransformOptions {
    firstOnly?: boolean;
    fontNames?: readonly string[];
    fontPattern?: RegExp;
}

// @public
export function createSeoPageDataBuilder(options: CreateSeoPageDataBuilderOptions): (input: Readonly<SeoPageData>) => SeoPageDataResult;

// @public
export interface CreateSeoPageDataBuilderOptions {
    author?: string;
    defaultImage?: string;
    description?: string;
    generateJsonLd?: (pageData: Readonly<SeoPageData>, context: SeoBuildContext) => JsonLdObject | undefined;
    includeMetaTitle?: boolean;
    includeSiteName?: boolean;
    includeTwitterDescription?: boolean;
    keywords?: readonly string[] | string;
    locale?: string;
    normalizeFrontmatter?: (pageData: Readonly<SeoPageData>) => SeoFrontmatterResult;
    preparePage?: (pageData: Readonly<SeoPageData>) => SeoPageData;
    resolveDescription?: (pageData: Readonly<SeoPageData>) => string;
    robotIndex?: string | number | boolean | null;
    siteName?: string;
    siteUrl: string | URL;
    stripText?: (value: string) => string;
}

// @public
export function createSitemapConfig<TItem extends SitemapItem = SitemapItem>(input: CreateSitemapConfigOptions<TItem>): {
    hostname: string;
    lastmodDateOnly: boolean;
    transformItems: (items: readonly TItem[]) => TItem[];
};

// @public
export interface CreateSitemapConfigOptions<TItem extends SitemapItem = SitemapItem> {
    formatUrl?: (url: string) => string;
    siteUrl: string | URL;
    transformItem?: (item: Readonly<TItem>) => TItem;
}

// @public
export type FontPreloadHeadEntry = [tag: 'link', attrs: Record<string, string>];

// @public
export type HeadAttrs = Readonly<Record<string, string | number | boolean>>;

// @public
export type HeadEntry = readonly [string, HeadAttrs?, string?];

// @public
export interface JsonLdObject {
    readonly [key: string]: JsonLdValue;
}

// @public
export type JsonLdPrimitive = string | number | boolean | null;

// @public
export type JsonLdValue = JsonLdPrimitive | JsonLdObject | readonly JsonLdValue[];

// @public
export function normalizeVitePressFrontmatter<TFrontmatter extends FrontmatterInput = FrontmatterInput>(page: VitePressPageData<TFrontmatter>, options?: NormalizeFrontmatterOptions): NormalizedFrontmatter<TFrontmatter>;

// @public
export function sanitizeHeadEntries(head: readonly unknown[]): readonly HeadEntry[];

// @public
export interface SeoBuildContext {
    readonly canonicalUrl: URL;
    readonly siteUrl: URL;
}

// @public
export interface SeoFrontmatterResult {
    readonly [key: string]: unknown;
    readonly url: string;
}

// @public
export interface SeoPageData {
    readonly description: string;
    readonly filePath: string;
    readonly frontmatter: Record<string, unknown>;
    readonly headers: SeoPageHeader[];
    readonly isNotFound?: boolean;
    readonly lastUpdated?: number;
    readonly params?: Record<string, unknown>;
    readonly relativePath: string;
    readonly title: string;
    readonly titleTemplate?: string | boolean;
}

// @public
export interface SeoPageDataResult extends SeoPageData {
    readonly frontmatter: Record<string, unknown> & {
        readonly head: readonly HeadEntry[];
    };
}

// @public
export interface SeoPageHeader {
    readonly children: SeoPageHeader[];
    readonly level: number;
    readonly link: string;
    readonly slug: string;
    readonly title: string;
}

// @public
export interface SitemapItem {
    readonly draft?: boolean;
    readonly url: string;
}

// @public
export interface TransformHeadContext {
    readonly assets: readonly string[];
}

// @public
export interface VitePressPageData<TFrontmatter extends FrontmatterInput = FrontmatterInput> extends PageDataLike<TFrontmatter> {
    readonly excerpt?: string;
    readonly frontmatter: TFrontmatter;
    readonly html?: string;
    readonly src?: string;
}

// (No @packageDocumentation comment for this package)

```
